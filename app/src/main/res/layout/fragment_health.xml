<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".home.fragment.HealthFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/content_health_fragment"
        android:orientation="vertical">
        <ProgressBar
            android:id="@+id/progress_bar_health"
            style="?android:attr/progressBarStyle"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_gravity="center|top"
            />
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view_health"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layoutAnimation="@anim/layout_animation_fall_down"
            />
    </LinearLayout>
    <include layout="@layout/no_elements" android:visibility="gone"></include>

</FrameLayout>